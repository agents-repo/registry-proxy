# SonarQube project settings (registry-proxy).
# Automatic Analysis ignores this file; it reads .sonarcloud.properties instead.
# scripts/lint-workflows.mjs resolves curl/tar/actionlint from trusted OS directories.
